apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard
  namespace: kubernetes-dashboard
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kong
  ports:
    - protocol: TCP
      port: 433
      targetPort: 8443
