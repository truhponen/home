---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: analytics
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs-csi
  volumeName: analytics
